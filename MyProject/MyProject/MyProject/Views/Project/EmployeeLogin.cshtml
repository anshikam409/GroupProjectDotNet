@model MyProject.Models.EmployeeDetails

@{
    ViewBag.Title = "EmployeeLogin";
}

<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #040D12;
            text-align: center;
            padding: 50px;
            color: #93B1A6;
        }

        h2 {
            color: #5C8374;
        }

        form {
            max-width: 300px;
            margin: 0 auto;
            background-color: #183D3D;
            padding: 20px;
            border-radius: 5px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        label {
            display: block;
            margin: 10px 0 5px;
            font-weight: bold;
            color: #93B1A6;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-bottom: 10px;
            box-sizing: border-box;
            background-color: #040D12;
            color: #93B1A6;
            border: 1px solid #5C8374;
        }

        button {
            background-color: #5C8374;
            color: #fff;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }

            button:hover {
                background-color: #183D3D;
            }

        .validation-summary-errors {
            color: #e74c3c;
        }

        .field-validation-error {
            color: #e74c3c;
        }

        .back-button {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: #5C8374;
            color: #fff;
            padding: 10px 20px;
            font-size: 16px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s ease;
            text-decoration: none;
        }

            .back-button:hover {
                background-color: #183D3D;
            }
    </style>
</head>
<body>
    <a href="@Url.Action("Index", "Project")" class="back-button">Back</a>
    <h2>Employee Login</h2>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        <form>
            <label for="Name">EmployeeId</label>
            @Html.TextBoxFor(model => model.EmpID, new { autocomplete = "off" })
            @Html.ValidationMessageFor(model => model.Employeename, "", new { @class = "text-danger" })

            <label for="Password">Password</label>
            @Html.PasswordFor(model => model.Password)
            @Html.ValidationMessageFor(model => model.Password, "", new { @class = "text-danger" })

            <button type="submit">Login</button>
            @Html.ValidationSummary(true, "", new { @class = "validation-summary-errors" })
        </form>
    }

    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
